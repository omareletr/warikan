import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omareletr.warikan',
  appName: 'Warikan',
  webDir: 'out',
  server: {
    hostname: 'warikan.local'
  }
};

export default config;
